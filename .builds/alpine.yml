image: alpine/latest
sources:
- https://git.sr.ht/~sircmpwn/harec
- https://git.sr.ht/~sircmpwn/qbe
tasks:
- qbe: |
    cd qbe
    make PREFIX=/usr
    sudo make install PREFIX=/usr
- prepare: |
    mkdir harec/build
    cd harec/build
    ../configure
- build: |
    cd harec/build
    make -j2
